// The watch communication counter example sends data (INT) from IOS to WatchOS and vice-versa.

// How to find the data-transfer time (millisecs) ? And also the timestamp has to be displayed on the application.
